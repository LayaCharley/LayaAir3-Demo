import { UIComponent } from "./UIComponent";
export declare class Box extends UIComponent {
    private _bgColor;
    set_dataSource(value: any): void;
    get bgColor(): string;
    set bgColor(value: string);
}
